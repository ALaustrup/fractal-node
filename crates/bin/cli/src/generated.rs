// GENERATED by `cargo xtask codegen` from crates/support/schema.
// Do not edit: your change will be overwritten and CI will fail on the diff.

/// A command, emitted from the contract. `fn schema` renders these, and the
/// dispatcher matches on `id` — so an operation cannot exist in the API and be
/// missing here (P13).
pub(crate) struct GeneratedCommand {
    pub id: &'static str,
    pub noun: &'static str,
    pub verb: &'static str,
    pub args: &'static [&'static str],
    pub flags: &'static [&'static str],
    pub summary: &'static str,
    pub method: &'static str,
    pub path: &'static str,
    pub dry_runnable: bool,
}

pub(crate) const COMMANDS: &[GeneratedCommand] = &[
    GeneratedCommand {
        id: "node.status",
        noun: "node",
        verb: "status",
        args: &[],
        flags: &[],
        summary: "Liveness and version of this Node.",
        method: "GET",
        path: "/health",
        dry_runnable: false,
    },
    GeneratedCommand {
        id: "node.meta",
        noun: "node",
        verb: "meta",
        args: &[],
        flags: &[],
        summary: "The machine-readable description of this Node's surface.",
        method: "GET",
        path: "/v1/meta",
        dry_runnable: false,
    },
    GeneratedCommand {
        id: "society.list",
        noun: "society",
        verb: "list",
        args: &[],
        flags: &[],
        summary: "Every Society this Node holds.",
        method: "GET",
        path: "/v1/societies",
        dry_runnable: false,
    },
    GeneratedCommand {
        id: "society.create",
        noun: "society",
        verb: "create",
        args: &["name"],
        flags: &["handle", "visibility", "idempotency-key"],
        summary: "Found a Society.",
        method: "POST",
        path: "/v1/societies",
        dry_runnable: true,
    },
    GeneratedCommand {
        id: "society.get",
        noun: "society",
        verb: "get",
        args: &["society_id"],
        flags: &[],
        summary: "Read one Society.",
        method: "GET",
        path: "/v1/societies/{society_id}",
        dry_runnable: false,
    },
];

/// Exit codes, from the contract. Stable forever: scripts depend on them.
pub(crate) const EXIT_CODES: &[(&str, i32)] = &[
    ("usage", 2),
    ("invalid_identifier", 2),
    ("invalid_handle", 2),
    ("invalid_fnid", 2),
    ("unauthenticated", 3),
    ("capability_denied", 4),
    ("confirmation_required", 9),
    ("not_found", 5),
    ("conflict", 6),
    ("rejected", 1),
    ("rate_limited", 7),
    ("store_unavailable", 8),
    ("unreachable", 8),
    ("internal", 70),
];

/// Look up the exit code for an error, defaulting to 1 (generic failure).
pub(crate) fn exit_code_for(code: &str) -> i32 {
    let mut i = 0;
    while i < EXIT_CODES.len() {
        if let Some((c, e)) = EXIT_CODES.get(i) {
            if *c == code {
                return *e;
            }
        }
        i += 1;
    }
    1
}
