[package]
name = "fractal-adapter-ambient-system"
description = "The real Clock, Rng and IdGen. The only place the system clock is read."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
fractal-types.workspace = true
fractal-ports.workspace = true
ulid = { version = "1", features = ["std"] }

[lints]
workspace = true
